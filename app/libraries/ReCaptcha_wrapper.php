<?php

require_once('ReCaptcha/autoload.php');

class ReCaptcha_wrapper {
/*
    private $recaptchaInstance;

    public function __construct()
    {
        $ci =& get_instance();
        $ci->config->load('secure');
        $this->recaptchaInstance = new \ReCaptcha\ReCaptcha($ci->config->item('recaptcha_secret_key'));
    }

    public function getRecaptchaInstance()
    { 
        return $this->recaptchaInstance;
    }*/
}
